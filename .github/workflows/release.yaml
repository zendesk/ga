name: Release

on:
  push:
    branches:
      - master

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - id: compute_tag
        uses: zendesk/compute-tag@v10
        with:
          github_token: ${{ github.token }}
          version_type: patch
